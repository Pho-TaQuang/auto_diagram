| Mảng                                       | Hiện trạng repo                                                                                                                                                                                                                  | Thiếu / chưa khớp với logic đã chốt                                                                                                                                                                             |
| ------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Segment overlap**                        | Router hiện vẫn có `canShareSegment`: cùng source hoặc cùng target thì được share segment.                                                                                                                                       | Thiếu rule mới: **mọi segment overlap đều illegal**, không có ngoại lệ same-source/same-target. Divider là cơ chế duy nhất để gom đường. ([GitHub][1])                                                          |
| **Divider planning**                       | Divider hiện group theo endpoint chung: `groupEdgesByEndpoint(edges, "sourceId")` và `"targetId"`.                                                                                                                               | Thiếu bucket `commonNode + remoteGroup`. Hiện có thể gom quá rộng, chưa phân biệt `Manager -> Entity` với `Manager -> Model`. ([GitHub][1])                                                                     |
| **Divider timing**                         | Trong `routeWithTemplateStrategy`, repo route edges trước, repair xong rồi mới `planRoutingDividers`.                                                                                                                            | Thiếu mô hình divider như một phần của routing graph ngay từ đầu. Với logic đã chốt, phải plan divider trước, expand thành connector vật lý, rồi route. ([GitHub][1])                                           |
| **Divider là obstacle**                    | Current template route check node hits dựa trên `document.nodes`; divider không nằm trong obstacle set khi route candidate.                                                                                                      | Thiếu rule: divider phải được đối xử như class node, không edge nào được đi xuyên qua divider interior. ([GitHub][1])                                                                                           |
| **Divider trunk thật**                     | README nói output có divider vertex và split visual connector thành class-to-divider/divider-to-class, nhưng template router hiện vẫn route semantic edges trước rồi mới materialize divider.                                    | Thiếu một pipeline thống nhất nơi semantic edge trong divider được expand thành `common -> divider` và `divider -> remote` **trước khi routing/validation**. ([GitHub][2])                                      |
| **Remote group side rule**                 | Divider side hiện chọn từ vị trí common node so với cluster rectangle.                                                                                                                                                           | Thiếu rule theo remote group: nếu group horizontal thì divider chỉ top/bottom; nếu vertical thì left/right.                                                                                                     |
| **Nhiều divider cùng remote group**        | README có nói tránh reuse side khi divider cùng cluster, nhưng không thấy model rõ cho `>2 divider` best-effort + warning trong template router. ([GitHub][2])                                                                   | Thiếu rule overflow: 2 divider dùng cạnh đối diện; >2 thì alternate side + offset + warning/diagnostic.                                                                                                         |
| **Anchor fallback**                        | `assignAnchors` chỉ tính anchor theo primary side từ `chooseAnchorSides`.                                                                                                                                                        | Thiếu anchor ratio cho mọi possible side. Khi candidate/fallback đổi side, không được rơi về center `0.5`; phải có bucket ratio theo `nodeId + side`. ([GitHub][1])                                             |
| **Source/target node hit**                 | `countEdgeNodeHits` bỏ qua source và target node.                                                                                                                                                                                | Thiếu validation “boundary-only”: endpoint được chạm boundary, nhưng segment không được đi xuyên interior của chính source/target. ([GitHub][1])                                                                |
| **Gap-between candidate**                  | Current candidate generation có hai midline template route và outer lanes. README gọi có direct/gutter/local under-row/exterior-lane, nhưng chưa thấy `gap-between` local giữa đúng 2 node theo rule mình đã chốt. ([GitHub][2]) | Thiếu candidate rõ ràng: nếu source nằm trên/dưới/trái/phải target và có gap sạch, route qua midpoint của gap đó trước corridor/outer.                                                                          |
| **Corridor/lane model**                    | Candidate hiện chủ yếu là template midX/midY và outer lane.                                                                                                                                                                      | Thiếu lane graph rõ nghĩa: gap lane, corridor lane, private offset lane, outer lane, divider lane; corridor/outer không được hiểu là shared segment.                                                            |
| **Obstacle index / segment index**         | Current code quét accepted paths trong `countIllegalSegmentOverlaps` và `countCrossingsWithAccepted`.                                                                                                                            | Thiếu index hóa segment/obstacle nếu muốn scale: horizontalByY, verticalByX, crossing index, obstacle spatial index. ([GitHub][1])                                                                              |
| **Hard-valid report semantics**            | Repo có structured run reports/trace theo README.                                                                                                                                                                                | Thiếu một summary đúng với hard rules mới: `segmentOverlaps` phải làm `hardValid=false`; `dividerNodeHits`, `endpointDividerInteriorHits`, `divider-side-overflow` cần thành diagnostics ổn định. ([GitHub][2]) |
| **Layout intent dạng rows/bands**          | Repo hiện vẫn có grid intent workflow và suggested architecture-spine placement. README nói layout JSON chỉnh grid size, group row/column, packing, class assignment. ([GitHub][2])                                              | Thiếu layout intent kiểu row/band linh hoạt như `controller imanager manager adapter / model dto entity` hoặc `entity / controller imanager manager adapter / model dto`.                                       |
| **Route ordering theo physical connector** | Current ordering route semantic edges trước, rồi dividers sau.                                                                                                                                                                   | Thiếu ordering mới: divider trunk trước, divider spokes sau, non-divider edges cuối.                                                                                                                            |
| **Validation cuối với divider**            | Routing validator hiện tồn tại, nhưng template source cho thấy hard failures trong router tính bằng node hits + illegal segment overlaps với accepted paths. ([GitHub][1])                                                       | Thiếu final validation thống nhất trên displayed connectors: class hits, divider hits, endpoint-divider interior hits, segment overlap tuyệt đối, crossing count.                                               |