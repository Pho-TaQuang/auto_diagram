# Slice 8: Default Switch

Artifacts:

- v2 default
- `--engine legacy` retained
- English README migration notes
- final compatibility notes

Gate:

- build/test pass
- CLI stable
- web ownership stable
- no blocker regression
- legacy fallback still works
